# Copy the contents of this file, and put it into your `~/.ssh/config` file.
# Make sure you replace the necessary parts (commented below).

Host *
    ServerAliveInterval 60
    ServerAliveCountMax 3

Host swarm-ingress
    HostName 78.128.235.199
    # Change to your user (e.g., `jkuzel`)
    User debian
    # Insert path to your private key (e.g., `~/.ssh/id_ed25519`)
    IdentityFile PLACEHOLDER

Host swarm-node-0
    HostName 172.16.2.87
    ProxyJump swarm-ingress
    # Change to your user (e.g., `jkuzel`)
    User debian
    # Insert path to your private key (e.g., `~/.ssh/id_ed25519`)
    IdentityFile PLACEHOLDER

Host swarm-node-1
    HostName 172.16.3.105
    ProxyJump swarm-ingress
    # Change to your user (e.g., `jkuzel`)
    User debian
    # Insert path to your private key (e.g., `~/.ssh/id_ed25519`)
    IdentityFile PLACEHOLDER

Host swarm-node-2
    HostName 172.16.5.152
    ProxyJump swarm-ingress
    # Change to your user (e.g., `jkuzel`)
    User debian
    # Insert path to your private key (e.g., `~/.ssh/id_ed25519`)
    IdentityFile PLACEHOLDER
