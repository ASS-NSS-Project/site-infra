# Copy the contents of this file, and put it into your `~/.ssh/config` file.
# Make sure you replace the necessary parts (commented below).

Host *
    ServerAliveInterval 60
    ServerAliveCountMax 3

Host swarm-manager-0
    HostName 78.128.235.16
    # Change to your user (e.g., `jkuzel`)
    User debian
    # Insert path to your private key (e.g., `~/.ssh/id_ed25519`)
    IdentityFile PLACEHOLDER

Host swarm-manager-1
    HostName 172.16.5.202
    ProxyJump swarm-manager-0
    # Change to your user (e.g., `jkuzel`)
    User debian
    # Insert path to your private key (e.g., `~/.ssh/id_ed25519`)
    IdentityFile PLACEHOLDER

Host swarm-manager-2
    HostName 172.16.4.77
    ProxyJump swarm-manager-0
    # Change to your user (e.g., `jkuzel`)
    User debian
    # Insert path to your private key (e.g., `~/.ssh/id_ed25519`)
    IdentityFile PLACEHOLDER

Host swarm-worker-0
    HostName 172.16.5.177
    ProxyJump swarm-manager-0
    # Change to your user (e.g., `jkuzel`)
    User debian
    # Insert path to your private key (e.g., `~/.ssh/id_ed25519`)
    IdentityFile PLACEHOLDER

Host swarm-worker-1
    HostName 172.16.1.62
    ProxyJump swarm-manager-0
    # Change to your user (e.g., `jkuzel`)
    User debian
    # Insert path to your private key (e.g., `~/.ssh/id_ed25519`)
    IdentityFile PLACEHOLDER

Host network-share-0
    HostName 172.16.2.192
    ProxyJump swarm-manager-0
    # Change to your user (e.g., `jkuzel`)
    User debian
    # Insert path to your private key (e.g., `~/.ssh/id_ed25519`)
    IdentityFile PLACEHOLDER

Host network-share-1
    HostName 172.16.2.63
    ProxyJump swarm-manager-0
    # Change to your user (e.g., `jkuzel`)
    User debian
    # Insert path to your private key (e.g., `~/.ssh/id_ed25519`)
    IdentityFile PLACEHOLDER

Host network-share-2
    HostName 172.16.3.86
    ProxyJump swarm-manager-0
    # Change to your user (e.g., `jkuzel`)
    User debian
    # Insert path to your private key (e.g., `~/.ssh/id_ed25519`)
    IdentityFile PLACEHOLDER