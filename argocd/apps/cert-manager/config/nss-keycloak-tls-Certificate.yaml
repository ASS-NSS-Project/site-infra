apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: nss-keycloak-tls
  namespace: traefik
spec:
  secretName: nss-keycloak-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - keycloak.nss.jkzl.eu
