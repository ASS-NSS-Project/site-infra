apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: nss-oauth2-tls
  namespace: traefik
spec:
  secretName: nss-oauth2-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - oauth2.nss.jkzl.eu
