apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: nss-rag-sys-tls
  namespace: traefik
spec:
  secretName: nss-rag-sys-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - rag.nss.jkzl.eu
