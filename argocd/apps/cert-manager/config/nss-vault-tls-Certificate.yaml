apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: nss-vault-tls
  namespace: traefik
spec:
  secretName: nss-vault-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - vault.nss.jkzl.eu
