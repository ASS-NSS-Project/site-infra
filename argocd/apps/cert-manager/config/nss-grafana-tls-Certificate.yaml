apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: nss-grafana-tls
  namespace: traefik
spec:
  secretName: nss-grafana-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - grafana.nss.jkzl.eu
