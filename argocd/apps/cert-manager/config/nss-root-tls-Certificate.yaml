apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: nss-root-tls
  namespace: traefik
spec:
  secretName: nss-root-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - nss.jkzl.eu
