apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: nss-qdrant-tls
  namespace: traefik
spec:
  secretName: nss-qdrant-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - qdrant.nss.jkzl.eu
