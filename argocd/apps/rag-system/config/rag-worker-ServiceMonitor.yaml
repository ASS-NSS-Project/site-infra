apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: rag-worker
  namespace: rag-system
  labels:
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app: rag-worker
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
