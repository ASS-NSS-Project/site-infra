apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: rag-worker-embed
  namespace: rag-system
  labels:
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app: rag-worker-embed
  endpoints:
    - port: metrics
      interval: 30s
