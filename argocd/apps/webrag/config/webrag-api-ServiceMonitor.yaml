apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: webrag-api
  namespace: webrag
  labels:
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app: webrag-api
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
