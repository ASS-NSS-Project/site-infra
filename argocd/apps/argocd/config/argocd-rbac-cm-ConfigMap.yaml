apiVersion: v1
kind: ConfigMap
metadata:
  name: argocd-rbac-cm
  namespace: argocd
data:
  # Members of the Keycloak "admins" group get full admin access.
  # Everyone else is read-only.
  policy.csv: |
    g, admins, role:admin
  policy.default: role:readonly
