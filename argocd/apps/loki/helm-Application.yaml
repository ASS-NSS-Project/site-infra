# Wave 17 — Loki log aggregation backend (after Alloy wave 16, before kube-prometheus-stack wave 18)
# No finalizer — Loki owns a Longhorn PVC for log chunks
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: loki-helm
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "17"
spec:
  revisionHistoryLimit: 0
  project: observability
  sources:
    - repoURL: https://grafana.github.io/helm-charts
      chart: loki
      targetRevision: ">=6.0.0"
      helm:
        releaseName: loki
        valueFiles:
          - $values/argocd/apps/loki/helm/values.yaml
    - repoURL: https://github.com/ASS-NSS-Project/site-infra
      targetRevision: kost
      ref: values
    - repoURL: https://github.com/ASS-NSS-Project/site-infra
      targetRevision: kost
      path: argocd/apps/loki/config
  destination:
    server: https://kubernetes.default.svc
    namespace: monitoring
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
