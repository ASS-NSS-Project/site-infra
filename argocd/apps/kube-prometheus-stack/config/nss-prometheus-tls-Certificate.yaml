apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: nss-prometheus-tls
  namespace: monitoring
spec:
  secretName: nss-prometheus-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - prometheus.nss.jkzl.eu
