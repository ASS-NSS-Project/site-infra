apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: monitoring-forward-auth
  namespace: monitoring
spec:
  forwardAuth:
    address: http://oauth2-proxy.traefik.svc.cluster.local:4180/oauth2/auth
    trustForwardHeader: true
    authResponseHeaders:
      - X-Auth-Request-User
      - X-Auth-Request-Email
      - Set-Cookie
---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: monitoring-oauth2-errors
  namespace: monitoring
spec:
  errors:
    status:
      - "403"
    service:
      name: oauth2-proxy
      namespace: traefik
      port: 4180
    query: /oauth2/sign_in?rd=https://prometheus.nss.jkzl.eu/
---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: monitoring-auth
  namespace: monitoring
spec:
  chain:
    middlewares:
      - name: monitoring-forward-auth
      - name: monitoring-oauth2-errors
