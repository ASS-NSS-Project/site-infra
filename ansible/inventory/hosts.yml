instances:
  children:
    swarm_managers:
      hosts:
        swarm_0:
          ansible_host: swarm-0
        swarm_1:
          ansible_host: swarm-1
        swarm_2:
          ansible_host: swarm-2

    backups:
      hosts:
        backup:
          ansible_host: backup

glusterfs_share_instances:
  children:
    swarm_managers:
